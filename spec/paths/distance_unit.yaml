get:
  tags:
    - Static Data
  responses:
    '200':
      description: fetch all the 'distance_unit' items
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/distance_unit'
