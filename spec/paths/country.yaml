get:
  tags:
    - Static Data
  responses:
    '200':
      description: fetch all the 'country' items
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/country'
